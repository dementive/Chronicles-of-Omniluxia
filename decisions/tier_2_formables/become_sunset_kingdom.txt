country_decisions = {
	
	form_sunset_kingdom = {
	
		potential = {
			num_of_cities >= 1
			tag = DN5
			NOR = {
				is_tier_1_formable_trigger = yes
				is_tier_2_formable_trigger = yes
				is_endgame_tag_trigger = yes
			}
		}
		
		highlight = {
			scope:province = {		
				is_in_region = dragon_lands_east
			}
		}
		
		allow = {
			custom_tooltip = {
				text = formable_not_sunset_kingdom_exists
				NOT = {
					any_country = {
						tag = SSS
					}
				}
			}
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = dragon_lands_east
		}

		
		effect = {
			custom_tooltip = "tier_2_formable_tooltip"
			change_country_name = "SSS"
			capital_scope = {
				establish_city_effect = yes
				capital_formable_medium_effect = yes
				formable_capital_modifier_normal_effect = yes
			}
			#Change up to 1,000 old culture pops to new culture
			hidden_effect = {
				every_owned_province = {
					while = {
						count = 1000
						every_pops_in_province = {
							limit = {
								culture = root.culture
							}
							set_pop_culture = sunset_elf
						}
					}
				}
				every_character = {
					limit = {
						AND = {
							OR = {
								from_ruler_family = yes
								is_minor_character = no
							}
							has_culture = root.culture
						}
					}
					set_culture = sunset_elf
				}
				set_primary_culture_cleanup_effect = {
					NEW_PRIMARY_CULTURE = sunset_elf
					MAKE_OLD_CULTURE_INTEGRATED = yes
				}
				change_country_adjective = "SSS_ADJ"
				change_country_color =  "sunset_yellow"
				change_country_tag = SSS
			}
			region:the_farlands_region = {
				every_region_province = {
					add_claim = root
				}
			}
			region:dragon_lands_west_region = {
				every_region_province = {
					add_claim = root
				}
			}
			add_3_free_province_investments = yes
			monarchy_formable_government_change_effect = yes
			change_government = theocratic_monarchy
			switch_government_type_event_clearup_effect = yes
			custom_tooltip = convert_to_sun_religion
	        hidden_effect = {
	            change_religion_effect = { 
	                RELIGION = sun_religion
	            }
	            add_country_modifier = {
	                name = sun_converts_mod
	                duration = 1200
	            }
	        }
		}
		
		
		ai_will_do = {
			base = 1
		}
	}
	
}