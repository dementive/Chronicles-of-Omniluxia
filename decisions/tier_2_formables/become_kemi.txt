country_decisions = {
	
	become_kemi = {
		
		potential = {
			num_of_cities >= 1
			OR = {
				tag = JMN
			}
		}
		
		highlight = {
			scope:province = {
				OR = {
					is_in_region = polaris_glaciers
					is_in_region = polaris_landhold
				}
			}
		}
		
		allow = {
			trigger_if = {
				limit = {
					any_country = {
						tag = PKM
					}
				}
				custom_tooltip = {
					text = formable_not_kemi_exists
					NOT = {
						any_country = {
							tag = PKM
						}
					}
				}
			}
			is_subject = no
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = polaris_glaciers
			owns_or_subject_owns_region = polaris_landhold
		}
		
		effect = {
			custom_tooltip = "tier_2_formable_tooltip"
			if = {
				limit = {
					NOR = { 
						government = imperium 
						government = imperial_cult
					}
				}
				republic_to_monarchy_law_variable_effect = yes # decide which legion law should be transferred
				change_government = imperium
				republic_to_monarchy_law_change_effect = yes # change legion law based on variable if present
				switch_government_type_event_clearup_effect = yes
			}
			change_country_name = "PKM"
			hidden_effect = {
				change_country_adjective = "PKMADJ"
				change_country_tag = PKM
			}
			add_5_free_province_investments = yes
			capital_scope = {
				capital_formable_large_effect = yes
				formable_capital_modifier_large_effect = yes
			}
			add_country_modifier = {
				name = formable_country_huge_modifier
				duration = 7600
			}
			mission_claim_region_effect = { REGION = horteon_north_region }
			mission_claim_region_effect = { REGION = horteon_shout_region }
			mission_claim_region_effect = { REGION = norrfalian_peninsula }
			mission_claim_region_effect = { REGION = norrfalian_borderland }
		}
		
		ai_will_do = {
			base = 1
		}
	}

}

