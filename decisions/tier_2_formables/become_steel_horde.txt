country_decisions = {
	
	become_steel_horde = {
	
		potential = {
			num_of_cities >= 1
			OR = {
				country_culture_group = selaskusian
				country_culture_group = alaguric
			}
			NOT = {
				tag = STH
			}
			NOR = {
				is_tier_2_formable_trigger = yes
				is_endgame_tag_trigger = yes
			}
		}
		
		highlight = {
			scope:province = {		
				OR = {
					is_in_region = coastland_sttepes
					is_in_region = north_sttepes
					is_in_region = central_sttepes
					is_in_region = westland_sttepes
					is_in_region = eastern_shout_skyreach_foothils
					is_in_region = zainddian_sttepes
					is_in_region = western_shout_skyreach_foothils
				}
			}
		}
		
		allow = {
			custom_tooltip = {
				text = formable_not_steel_horde_exists
				NOT = {
					any_country = {
						tag = STH
					}
				}
			}
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = coastland_sttepes
			owns_or_subject_owns_region = north_sttepes
			owns_or_subject_owns_region = central_sttepes
			owns_or_subject_owns_region = westland_sttepes
			owns_or_subject_owns_region = eastern_shout_skyreach_foothils
			owns_or_subject_owns_region = zainddian_sttepes
			owns_or_subject_owns_region = western_shout_skyreach_foothils
		}
		
		effect = {
			custom_tooltip = "tier_2_formable_tooltip"	
			if = {
				limit = {
					NOR = { 
						government = imperium 
						government = imperial_cult
					}
				}
				republic_to_monarchy_law_variable_effect = yes # decide which legion law should be transferred
				change_government = imperium
				republic_to_monarchy_law_change_effect = yes # change legion law based on variable if present
				switch_government_type_event_clearup_effect = yes
			}
			hidden_effect = {
				change_country_adjective = "STHADJ"
				change_country_tag = STH
            }
            add_5_free_province_investments = yes
            capital_scope = {
            	capital_formable_large_effect = yes
            	formable_capital_modifier_large_effect = yes
            }
            add_country_modifier = {
            	name = formable_country_huge_modifier
            	duration = 7600
            }
		}
		
		ai_will_do = {
			base = 1
		}
	}
	
}