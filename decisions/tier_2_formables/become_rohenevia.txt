country_decisions = {
	
	become_rohenevia = {
		
		potential = {
			num_of_cities >= 1
			OR = {
				country_culture_group = aralan_rohenoan
				country_culture_group = rohenoan
				country_culture_group = aralans
				country_culture_group = zani_vetalian
			}
			NOR = {
				is_tier_2_formable_trigger = yes
				is_endgame_tag_trigger = yes
			}
		}
		
		highlight = {
			scope:province = {
				OR = {
					is_in_region = rohenoa_central_region
					is_in_region = rohevia_region
				}
			}
		}
		
		allow = {
			trigger_if = {
				limit = {
					any_country = {
						tag = RHP
					}
				}
				custom_tooltip = {
					text = formable_not_rohenevia_exists
					NOT = {
						any_country = {
							tag = RHP
						}
					}
				}
			}
			is_subject = no
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = rohenoa_central_region
			owns_or_subject_owns_region = rohevia_region
		}
		
		effect = {
			custom_tooltip = "tier_3_formable_tooltip"
			if = {
				limit = {
					NOR = { 
						government = imperium 
						government = imperial_cult
					}
				}
				republic_to_monarchy_law_variable_effect = yes # decide which legion law should be transferred
				change_government = imperium
				republic_to_monarchy_law_change_effect = yes # change legion law based on variable if present
				switch_government_type_event_clearup_effect = yes
			}
			change_country_name = "RHP"
			hidden_effect = {
				change_country_adjective = "RHPADJ"
				change_country_tag = RHP
				change_country_flag = RHP_FLAG
			}
			add_5_free_province_investments = yes
			capital_scope = {
				capital_formable_large_effect = yes
				formable_capital_modifier_large_effect = yes
			}
			add_country_modifier = {
				name = formable_country_huge_modifier
				duration = 7600
			}
			mission_claim_region_effect = { REGION = celtica_north_region }
			mission_claim_region_effect = { REGION = bloody_islands_rendun_region }
			mission_claim_region_effect = { REGION = bloody_islands_remun_region }
			mission_claim_region_effect = { REGION = gelica_region }
		}
		
		ai_will_do = {
			base = 1
		}
	}

}

