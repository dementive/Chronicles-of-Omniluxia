country_decisions = {
	
	become_orconia = {
	
		potential = {
			num_of_cities >= 1
			this.culture = culture:orcish_5
			NOT = {
				tag = OCA
			}
			NOR = {
				is_tier_2_formable_trigger = yes
				is_endgame_tag_trigger = yes
			}
		}
		
		highlight = {
			scope:province = {		
				OR = {
					is_in_region = border_lands_morrigon
					is_in_region = old_fronlines_region
					is_in_region = gelica_region
				}
			}
		}
		
		allow = {
			custom_tooltip = {
				text = formable_not_orconia_exists
				NOT = {
					any_country = {
						tag = OCA
					}
				}
			}
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = border_lands_morrigon
			owns_or_subject_owns_region = old_fronlines_region
			owns_or_subject_owns_region = gelica_region
		}
		
		effect = {
			custom_tooltip = "tier_2_formable_tooltip"		
			change_country_name = "OCA"
			hidden_effect = {
				change_country_adjective = "OCAADJ"
				change_country_tag = OCA
				change_country_color = "pitch_black"
				change_country_flag = "OCA"
            }
			add_5_free_province_investments = yes
		    capital_scope = {
				capital_formable_medium_effect = yes
				formable_capital_modifier_normal_effect = yes
		    }
			mission_claim_region_effect = { REGION = wegalia_north_region }
			mission_claim_region_effect = { REGION = wegalia_shout_region }
			mission_claim_region_effect = { REGION = bloody_islands_rendun_region }
			mission_claim_region_effect = { REGION = marshland_region }
			mission_claim_region_effect = { REGION = celtica_north_region }
			mission_claim_region_effect = { REGION = rohevia_region }
		}
		
		ai_will_do = {
			base = 1
		}
	}
	
}