country_decisions = {
	
	become_celtica = {
	
		potential = {
			num_of_cities >= 1
			country_culture_group = celtican
			NOT = {
				tag = CLT
			}
			NOR = {
				is_tier_1_formable_trigger = yes
				is_tier_2_formable_trigger = yes
				is_endgame_tag_trigger = yes
			}
		}
		
		highlight = {
			scope:province = {		
				OR = {
					is_in_region = celtica_north_region
					is_in_region = celtica_shout_region
					is_in_region = gelica_region
				}
			}
		}
		
		allow = {
			custom_tooltip = {
				text = formable_not_celtica_exists
				NOT = {
					any_country = {
						tag = CLT
					}				
				}
			}
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = celtica_north_region
			owns_or_subject_owns_region = celtica_shout_region
			owns_or_subject_owns_region = gelica_region
		}
		
		effect = {
			custom_tooltip = "tier_1_formable_tooltip"		
			change_country_name = "CLT"
			add_4_free_province_investments = yes
			hidden_effect = {
				change_country_adjective = "CLT_ADJ"
				change_country_color = "frank_green"
				change_country_tag = CLT
				change_country_flag = "CLT_FLAG"
			}
			mission_claim_region_effect = { REGION = old_fronlines_region }
			mission_claim_region_effect = { REGION = dragon_pass }
			mission_claim_region_effect = { REGION = vetalia_north_region }
			mission_claim_region_effect = { REGION = vetalia_west_region }
			capital_scope = {
				capital_formable_large_effect = yes
				formable_capital_modifier_large_effect = yes
			}
			add_country_modifier = {
				name = formable_country_huge_modifier
				duration = 7600
			}
		}
		
		
		ai_will_do = {
			base = 10
		}
	}
	
}