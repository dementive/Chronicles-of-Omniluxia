country_decisions = {
	
	#form Zaraken Empire
	form_zaraken_empire = {
	
		potential = {
			num_of_cities >= 1
			this.culture.culture_group = culture_group:zarakens
			NOT = {
				tag = ZAE
			}
			NOR = {
				is_tier_1_formable_trigger = yes
				is_tier_2_formable_trigger = yes
				is_endgame_tag_trigger = yes
			}
		}
		
		highlight = {
			scope:province = {
				is_in_region = golden_foothills_eteradica
			}
		}
		
		allow = {
			custom_tooltip = {
				text = formable_not_zaraken_empire_exists
				NOT = {
					any_country = {
						tag = ZAE
					}
				}
			}
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = golden_foothills_eteradica
		}

		
		effect = {
			custom_tooltip = "tier_1_formable_tooltip"		
			change_country_name = "ZAE"
			hidden_effect = {
				change_country_adjective = "ZAE_ADJ"
				change_country_color = "zaraken_empire_color"
				change_country_tag = ZAE
				change_country_flag = "ZAE_FLAG"
				mission_claim_region_effect = { REGION = zainddian_western_eteradica }
				mission_claim_region_effect = { REGION = zainddian_eastern_eteradica }
			}
			if = {
				limit = {
					NOR = { 
						government = imperium 
						government = imperial_cult
					}
				}
				republic_to_monarchy_law_variable_effect = yes # decide which legion law should be transferred
				change_government = imperium
				republic_to_monarchy_law_change_effect = yes # change legion law based on variable if present
				switch_government_type_event_clearup_effect = yes
			}
			set_primary_culture_cleanup_effect = {
				NEW_PRIMARY_CULTURE = zaraken_1
				MAKE_OLD_CULTURE_INTEGRATED = yes
			}
			add_4_free_province_investments = yes
		}
		
		
		ai_will_do = {
			base = 10
		}
	}
	
}