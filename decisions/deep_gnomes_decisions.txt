country_decisions = {
	
	# Form Gaaron
	form_gaaron = {
	
		potential = {
			num_of_cities >= 1
			OR = {
				tag = DR2
				tag = DG1
				tag = DG2
			}
			NOT = {
				tag = GAA
			}
			NOR = {
				is_tier_2_formable_trigger = yes
				is_endgame_tag_trigger = yes
			}
		}
		highlight = {
			scope:province = {
				OR = {
					province_id = 1634
					province_id = 1613
					province_id = 1587
					province_id = 1667
					province_id = 1581
				}
			}
		}
		allow = {
			custom_tooltip = {
				text = formable_not_form_gaaron_exists
				NOT = {
					any_country = {
						tag = GAA
					}
				}
			}
			owns = 1634
			owns = 1613
			owns = 1587
			owns = 1667
			owns = 1581
		}
		
		effect = {
			add_treasury = 200
			add_political_influence = 50

			custom_tooltip = "tier_2_formable_tooltip"
			if = {
				limit = {
					NOR = { 
						government = tribal_chiefdom
					}
				}
				change_government = tribal_chiefdom
				switch_government_type_event_clearup_effect = yes
			}
			change_country_name = "GAA"
			hidden_effect = {
				change_country_adjective = "GAAADJ"
				change_country_tag = GAA
			}
			add_5_free_province_investments = yes
			capital_scope = {
				capital_formable_large_effect = yes
				formable_capital_modifier_large_effect = yes
			}
			add_country_modifier = {
				name = formable_country_huge_modifier
				duration = 7600
			}
			mission_claim_region_effect = { REGION = deep_rift_valeys_region }
			mission_claim_region_effect = { REGION = errnorfallie_north }
			mission_claim_region_effect = { REGION = east_wild_northland }

			hidden_effect = {
				create_unit = {
	                navy = no
	                name = "Gaaron Horde"
	                location = capital_scope
	                hidden_effect = {
	                    add_subunit = light_infantry
	                    add_subunit = light_infantry
	                    add_subunit = light_infantry
	                    add_subunit = light_infantry
	                    add_subunit = light_infantry  
	                    add_subunit = light_infantry
	                    add_subunit = light_infantry  
	                    add_subunit = light_infantry
	                    add_subunit = light_infantry  
	                    add_subunit = light_infantry 
	                    add_subunit = supply_train                                
	                    add_food = 150
	                    add_morale = 3
	                }
            	}
            	create_unit = {
	                navy = no
	                name = "Gaaron Horde"
	                location = capital_scope
	                hidden_effect = {
	                    add_subunit = light_infantry
	                    add_subunit = light_infantry
	                    add_subunit = light_infantry
	                    add_subunit = light_infantry
	                    add_subunit = light_infantry  
	                    add_subunit = light_infantry
	                    add_subunit = light_infantry  
	                    add_subunit = light_infantry
	                    add_subunit = light_infantry  
	                    add_subunit = light_infantry 
	                    add_subunit = supply_train                                
	                    add_food = 150
	                    add_morale = 3
	                }
            	}

				every_country = {
	            	limit = {
	            		NOT = {
	            			OR = {
	            				country_culture_group = deep_gnome
	            			}
	            		}
	            	}
	                trigger_event = deepgnome.2
	            }
	        }
		}
		
		ai_will_do = {
			base = 100
		}
	}
	
}
