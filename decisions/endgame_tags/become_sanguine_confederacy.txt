country_decisions = {
	
	become_sanguine_confederacy = {
		
		potential = {
			num_of_cities >= 1
			country_culture_group = drow
		}
		
		highlight = {
			scope:province = {
				OR = {
					is_in_region = norrfalian_peninsula
					is_in_region = norrfalian_balthazaria
					is_in_region = norrfalian_borderland
					is_in_region = west_wild_northland
					is_in_region = east_wild_northland
				}
			}
		}
		
		allow = {
			trigger_if = {
				limit = {
					any_country = {
						tag = SNG
					}
				}
				custom_tooltip = {
					text = sanguine_confederacy_not_existing
					NOT = {
						any_country = {
							tag = SNG
						}
					}
				}
			}
			is_subject = no
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = norrfalian_peninsula
			owns_or_subject_owns_region = norrfalian_balthazaria
			owns_or_subject_owns_region = norrfalian_borderland
			owns_or_subject_owns_region = west_wild_northland
			owns_or_subject_owns_region = east_wild_northland
		}
		
		
		effect = {
			trigger_event = me_omnispecial.10
			custom_tooltip = "tier_3_formable_tooltip"
			if = {
				limit = {
					NOR = { 
						government = imperium 
						government = imperial_cult
					}
				}
				republic_to_monarchy_law_variable_effect = yes # decide which legion law should be transferred
				change_government = imperium
				republic_to_monarchy_law_change_effect = yes # change legion law based on variable if present
				switch_government_type_event_clearup_effect = yes
			}
			change_country_name = "SNG"
			hidden_effect = {
				change_country_adjective = "SNGADJ"
				change_country_color = "navy_blue"
				change_country_tag = SNG
				change_country_flag = DE3
			}
			add_5_free_province_investments = yes
			capital_scope = {
				capital_formable_large_effect = yes
				formable_capital_modifier_large_effect = yes
			}
			add_country_modifier = {
				name = formable_country_huge_modifier
				duration = 7600
			}
			mission_claim_region_effect = { REGION = errnorfalie_north }
			mission_claim_region_effect = { REGION = deep_rift_valeys_region }
		}
		
		ai_will_do = {
			base = 1
		}
	}

}

