country_decisions = {
	
	become_three_crowns = {
	
		potential = {
			num_of_cities >= 1
			OR = {
				country_culture_group = western_zerywani
				country_culture_group = eastern_zerywani
			}
			NOT = {
				tag = TCR
			}
			NOR = {
				is_tier_2_formable_trigger = yes
				is_endgame_tag_trigger = yes
			}
		}
		
		highlight = {
			scope:province = {		
				OR = {
					is_in_region = pomorania_region
					is_in_region = russhovia_region
					is_in_region = zarusshovia_region
					is_in_region = mizhykraina_region
					is_in_region = dolny_zerywani
					is_in_region = lechia_region
					is_in_region = polabowy_region
				}
			}
		}
		
		allow = {
			custom_tooltip = {
				text = formable_not_three_crowns_exists
				NOT = {
					any_country = {
						tag = TCR
					}
				}
			}
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = pomorania_region
			owns_or_subject_owns_region = russhovia_region
			owns_or_subject_owns_region = zarusshovia_region
			owns_or_subject_owns_region = mizhykraina_region
			owns_or_subject_owns_region = dolny_zerywani
			owns_or_subject_owns_region = lechia_region
			owns_or_subject_owns_region = polabowy_region
		}
		
		effect = {
			custom_tooltip = "tier_2_formable_tooltip"	
			if = {
				limit = {
					NOR = { 
						government = imperium 
						government = imperial_cult
					}
				}
				republic_to_monarchy_law_variable_effect = yes # decide which legion law should be transferred
				change_government = imperium
				republic_to_monarchy_law_change_effect = yes # change legion law based on variable if present
				switch_government_type_event_clearup_effect = yes
			}
			hidden_effect = {
				change_country_adjective = "TCRADJ"
				change_country_tag = TCR
            }
            add_5_free_province_investments = yes
            capital_scope = {
            	capital_formable_large_effect = yes
            	formable_capital_modifier_large_effect = yes
            }
            add_country_modifier = {
            	name = formable_country_huge_modifier
            	duration = 7600
            }
            mission_claim_region_effect = { REGION = central_flusenland }
            mission_claim_region_effect = { REGION = shout_flusenland }
            mission_claim_region_effect = { REGION = north_sttepes }
            mission_claim_region_effect = { REGION = central_sttepes }
            mission_claim_region_effect = { REGION = coastland_sttepes }
		}
		
		ai_will_do = {
			base = 1
		}
	}
	
}