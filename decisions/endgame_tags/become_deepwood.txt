country_decisions = {
	
	become_deepwood = {
		
		potential = {
			num_of_cities >= 1
			primary_culture = forest_elves_1
		}
		
		highlight = {
			scope:province = {
				OR = {
					is_in_region = peaceful_valeys_region
					is_in_region = worlds_lungs_forest_region
				}
			}
		}
		
		allow = {
			trigger_if = {
				limit = {
					any_country = {
						tag = DPW
					}
				}
				custom_tooltip = {
					text = formable_not_deepwood_exists
					NOT = {
						any_country = {
							tag = DPW
						}
					}
				}
			}
			is_subject = no
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = peaceful_valeys_region
			owns_or_subject_owns_region = worlds_lungs_forest_region
		}
		
		effect = {
			custom_tooltip = "tier_3_formable_tooltip"
			if = {
				limit = {
					NOR = { 
						government = imperium 
						government = imperial_cult
					}
				}
				republic_to_monarchy_law_variable_effect = yes # decide which legion law should be transferred
				change_government = imperium
				republic_to_monarchy_law_change_effect = yes # change legion law based on variable if present
				switch_government_type_event_clearup_effect = yes
			}
			change_country_name = "DPW"
			hidden_effect = {
				change_country_adjective = "DPWADJ"
				change_country_tag = DPW
			}
			add_5_free_province_investments = yes
			capital_scope = {
				capital_formable_large_effect = yes
				formable_capital_modifier_large_effect = yes
			}
			add_country_modifier = {
				name = formable_country_huge_modifier
				duration = 7600
			}
			mission_claim_region_effect = { REGION = north_flusenland }
			mission_claim_region_effect = { REGION = deep_rift_valeys_region }
			mission_claim_region_effect = { REGION = east_wild_northland }
		}
		
		ai_will_do = {
			base = 1
		}
	}

}

