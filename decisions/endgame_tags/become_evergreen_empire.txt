country_decisions = {
	
	become_evergreen_empire = {
		
		potential = {
			num_of_cities >= 1
			country_culture_group = morrigon_forest_elves
		}
		
		highlight = {
			scope:province = {
				OR = {
					is_in_region = the_farlands
					is_in_region = vetalia_east_region
					is_in_region = vetalia_north_region
					is_in_region = vetalia_west_region
					is_in_region = green_valey_region
					is_in_region = celtica_shout_region
					is_in_region = celtica_north_region
					is_in_region = dragon_lands_east
					is_in_region = dragon_pass
				}
			}
		}
		
		allow = {
			trigger_if = {
				limit = {
					any_country = {
						tag = EGE
					}
				}
				custom_tooltip = {
					text = formable_not_evergreen_exists
					NOT = {
						any_country = {
							tag = EGE
						}
					}
				}
			}
			is_subject = no
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = the_farlands
			owns_or_subject_owns_region = vetalia_east_region
			owns_or_subject_owns_region = vetalia_north_region
			owns_or_subject_owns_region = vetalia_west_region
			owns_or_subject_owns_region = green_valey_region
			owns_or_subject_owns_region = celtica_shout_region
			owns_or_subject_owns_region = celtica_north_region
			owns_or_subject_owns_region = dragon_lands_east
			owns_or_subject_owns_region = dragon_pass
		}
		
		effect = {
			custom_tooltip = "tier_3_formable_tooltip"
			if = {
				limit = {
					NOR = { 
						government = imperium 
						government = imperial_cult
					}
				}
				republic_to_monarchy_law_variable_effect = yes # decide which legion law should be transferred
				change_government = imperium
				republic_to_monarchy_law_change_effect = yes # change legion law based on variable if present
				switch_government_type_event_clearup_effect = yes
			}
			change_country_name = "EGE"
			hidden_effect = {
				change_country_adjective = "EGEADJ"
				change_country_tag = EGE
			}
			add_5_free_province_investments = yes
			capital_scope = {
				capital_formable_large_effect = yes
				formable_capital_modifier_large_effect = yes
			}
			add_country_modifier = {
				name = formable_country_huge_modifier
				duration = 7600
			}
			mission_claim_region_effect = { REGION = dragon_lands_west }
			mission_claim_region_effect = { REGION = marshland_region }
			mission_claim_region_effect = { REGION = gelica_region }
			mission_claim_region_effect = { REGION = rohevia_region }
		}
		
		ai_will_do = {
			base = 1
		}
	}

}

