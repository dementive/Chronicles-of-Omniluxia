country_decisions = {
	
	become_empire_of_ular_pria = {
		
		potential = {
			num_of_cities >= 1
			country_culture_group = snakefolk
			NOT = {
				tag = ULP
			}	
		}
		
		highlight = {
			scope:province = {
				OR = {
					is_in_region = northern_austropetolian_isle
					is_in_region = southern_austropetolian_isle
					is_in_region = austropetolian_archipelago
				}
			}
		}
		
		allow = {
			trigger_if = {
				limit = {
					any_country = {
						tag = ULP
					}
				}
				custom_tooltip = {
					text = formable_not_empire_of_ular_pria_exists
					NOT = {
						any_country = {
							tag = ULP
						}
					}
				}
			}
			is_subject = no
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = northern_austropetolian_isle
			owns_or_subject_owns_region = southern_austropetolian_isle
			owns_or_subject_owns_region = austropetolian_archipelago
		}
		
		
		effect = {
			custom_tooltip = "tier_3_formable_tooltip"
			if = {
				limit = {
					NOR = { 
						government = imperium 
						government = imperial_cult
					}
				}
				republic_to_monarchy_law_variable_effect = yes # decide which legion law should be transferred
				change_government = imperium
				republic_to_monarchy_law_change_effect = yes # change legion law based on variable if present
				switch_government_type_event_clearup_effect = yes
			}
			change_country_name = "ULP"
			hidden_effect = {
				change_country_adjective = "ULPADJ"
				change_country_color = "ck2_green"
				change_country_tag = ULP
				change_country_flag = ULP
			}
			add_5_free_province_investments = yes
			capital_scope = {
				capital_formable_large_effect = yes
				formable_capital_modifier_large_effect = yes
			}
			add_country_modifier = {
				name = formable_country_huge_modifier
				duration = 7600
			}
			trigger_event = me_mignadg.14
		}
	}
}
