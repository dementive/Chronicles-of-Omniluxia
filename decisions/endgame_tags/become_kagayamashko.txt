country_decisions = {
	
	form_kagayamashko = {
		
		potential = {
			num_of_cities >= 1
			OR = {
				religion = the_great_mycelium_imperial
			}
			NOT = {
				tag = KGY
			}			
		}
		
		highlight = {
			scope:province = {
				OR = {
					is_in_region = arame_island_north_region
					is_in_region = arame_island_west_region
					is_in_region = arame_island_east_region
				}
			}
		}
		
		allow = {
			trigger_if = {
				limit = {
					exists = c:KGY
				}
				custom_tooltip = {
					text = kagayamashko_not_existing
					NOT = {
						exists = c:KGY
					}
				}
			}
			is_subject = no
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = arame_island_north_region
			owns_or_subject_owns_region = arame_island_west_region
			owns_or_subject_owns_region = arame_island_east_region
		}
		
		
		effect = {
			custom_tooltip = "tier_3_formable_tooltip"
			if = {
				limit = {
					NOR = { 
						government = imperium 
						government = imperial_cult
					}
				}
				republic_to_monarchy_law_variable_effect = yes # decide which legion law should be transferred
				change_government = imperium
				republic_to_monarchy_law_change_effect = yes # change legion law based on variable if present
				switch_government_type_event_clearup_effect = yes
			}
			change_country_name = "KGY"
			hidden_effect = {
				change_country_adjective = "KGY_ADJECTIVE"
				change_country_color = "ck2_purple"
				change_country_tag = KGY
				change_country_flag = KGY_FLAG
			}
			add_5_free_province_investments = yes
			capital_scope = {
				capital_formable_large_effect = yes
				formable_capital_modifier_large_effect = yes
			}
			add_country_modifier = {
				name = formable_country_huge_modifier
				duration = 7600
			}
			trigger_event = me_omnispecial.1
		}
		
		ai_will_do = {
			base = 1
		}
	}

}

