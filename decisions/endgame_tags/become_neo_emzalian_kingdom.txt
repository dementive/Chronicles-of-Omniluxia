country_decisions = {
	
	become_neo_emzalian_kingdom = {
		
		potential = {
			num_of_cities >= 1
			primary_culture = marshlander
		}
		
		highlight = {
			scope:province = {
				OR = {
					is_in_region = old_fronlines_region
					is_in_region = marshland_region
				}
			}
		}
		
		allow = {
			trigger_if = {
				limit = {
					any_country = {
						tag = NEK
					}
				}
				custom_tooltip = {
					text = formable_not_emzalia_exists
					NOT = {
						any_country = {
							tag = NEK
						}
					}
				}
			}
			is_subject = no
			can_form_nation_trigger = yes
			owns_or_subject_owns_region = old_fronlines_region
			owns_or_subject_owns_region = marshland_region
		}
		
		effect = {
			custom_tooltip = "tier_3_formable_tooltip"
			change_country_name = "NEK"
			hidden_effect = {
				change_country_adjective = "NEKADJ"
				change_country_tag = NEK
			}
			add_5_free_province_investments = yes
			capital_scope = {
				capital_formable_large_effect = yes
				formable_capital_modifier_large_effect = yes
			}
			add_country_modifier = {
				name = formable_country_huge_modifier
				duration = 7600
			}
		}
		
		ai_will_do = {
			base = 1
		}
	}

}

