
construct_buildings = {
    base = build_in_province
    layout = build_in_province
	
	map_mode = macro_builder_build_in_province

    includes = buildings
	
	sound = "event:/SFX/UI/Province/sfx_ui_province_build_building"
}

recruit_units = {
    base = build_in_province
    layout = build_in_province
	
	map_mode = macro_builder_build_in_province

    includes = {
        cohorts
        ships
    }
	
	sound = "event:/SFX/UI/Province/sfx_ui_province_build_unit"
}

promote_pops = {
    base = build_in_province
    layout = build_in_province
#    map_mode = population
	
	map_mode = macro_builder_build_in_province

    includes = promote_pops

	sound = "event:/SFX/UI/Province/sfx_ui_province_pop_promote"
}

convert_pops = {
    base = build_in_province
    layout = build_in_province
#    map_mode = population
	
	map_mode = macro_builder_build_in_province

    includes = convert_pops

	sound = "event:/SFX/UI/Province/sfx_ui_province_pop_convert"
}


assimilate_pops = {
    base = build_in_province
    layout = build_in_province
#    map_mode = population
	
	map_mode = macro_builder_build_in_province

    includes = assimilate_pops

	sound = "event:/SFX/UI/Province/sfx_ui_province_pop_convert"
}


#	sound = "event:/SFX/UI/Province/sfx_ui_province_pop_move"

import_tradegood = {
    base = trade_goods
    layout = trade_goods
    map_mode = trade
	
	includes = all

	sound = "event:/SFX/UI/Trade/sfx_ui_trade_trade"
}
