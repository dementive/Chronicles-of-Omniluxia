namespace = scheme_mana

# Focused Meditation — moderate mana gain
scheme_mana.1 = {
	type = character_event

	title = scheme_mana.1_title
	desc = scheme_mana.1_desc
	picture = ascetic
	left_portrait = scope:actor

	option = {
		name = scheme_mana.1.a
		change_character_magic = { VALUE = 15 }
		custom_tooltip = scheme_mana_gain_15_tt
	}
}

# Deep Trance — larger mana gain
scheme_mana.2 = {
	type = character_event

	title = scheme_mana.2_title
	desc = scheme_mana.2_desc
	picture = ascetic
	left_portrait = scope:actor

	option = {
		name = scheme_mana.2.a
		change_character_magic = { VALUE = 25 }
		custom_tooltip = scheme_mana_gain_25_tt
	}
}

# Interrupted Study — small gain, flavor
scheme_mana.3 = {
	type = character_event

	title = scheme_mana.3_title
	desc = scheme_mana.3_desc
	picture = ascetic
	left_portrait = scope:actor

	option = {
		name = scheme_mana.3.a
		change_character_magic = { VALUE = 5 }
		custom_tooltip = scheme_mana_gain_5_tt
	}
}
