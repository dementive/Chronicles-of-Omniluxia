# OMNILUXIA CUSTOM ACHIEVEMENTS - TEXT FORMATTING
# Adds the #huge text tag used by the achievement unlock toast description.
textformatting = {
    format = {
        name = huge
        format = "size:17"
    }
}
