magic_monthly_effects = {
	change_country_scope_magic_vars = yes
}

# NOTE (July 2026): magic_monthly and magic_max are display-only copies of the
# modifier-derived script values (omni_character_monthly_mana_value /
# omni_character_magic_max_value). The tick regenerates them from modifiers, so
# any hand-written change_variable bonus to them is wiped at the next refresh
# and never has a mechanical effect (see PERFORMANCE_AUDIT.md). They must stay
# STORED because this Imperator version's data system has no ScriptValue loc
# function - the mana tooltips can only read Var('magic_monthly')/Var('magic_max').
# They refresh monthly with the mana tick.
recalculate_character_magic_vars = {
	if = {
		limit = { NOT = { has_variable = magic } }
		set_variable = {
			name = magic
			value = 0
		}
	}
	set_variable = {
		name = magic_monthly
		value = omni_character_monthly_mana_value
	}
	set_variable = {
		name = magic_max
		value = omni_character_magic_max_value
	}
}

refill_character_magic_to_max = {
	recalculate_character_magic_vars = yes
	set_variable = {
		name = magic
		value = omni_character_magic_max_value
	}
}

magic_on_birth_effects = {
	if = {
		limit = {
			NOT = { has_variable = magic }
		}
		set_variable = {
			name = magic
			value = {
				integer_range = { min = 1 max = 10 }
			}
		}
	}
	recalculate_character_magic_vars = yes
}

magic_game_start_effects = {
	if = {
		limit = {
			NOT = { has_global_variable = magic_setup_done }
		}
		set_magic_vars = yes
	}
	set_global_variable = magic_setup_done
}

set_magic_vars = {
	every_country = {
		if = {
			limit = {
				NOT = { has_variable = magic }
			}
			set_variable = {
				name = magic
				value = 0
			}
		}
	}
	every_living_character = {
		if = {
			limit = {
				NOT = { has_variable = magic }
			}
			set_variable = {
				name = magic
				value = 0
			}
		}
		recalculate_character_magic_vars = yes
	}
}

change_country_magic = {
	change_variable = {
		name = magic
		add = $VALUE$
	}
	if = {
		limit = { var:magic > 100 }
		set_variable = {
			name = magic
			value = 100
		}
	}
}

change_province_magic = {
	change_variable = {
		name = magic
		add = $VALUE$
	}
	if = {
		limit = { var:magic > 100 }
		set_variable = {
			name = magic
			value = 100
		}
	}
}

clamp_character_magic_to_max = {
	if = {
		limit = {
			has_variable = magic
			var:magic > omni_character_magic_max_value
		}
		set_variable = {
			name = magic
			value = omni_character_magic_max_value
		}
	}
}

change_character_magic = {
	if = {
		limit = { NOT = { has_variable = magic } }
		set_variable = {
			name = magic
			value = 0
		}
	}
	change_variable = {
		name = magic
		add = $VALUE$
	}
	clamp_character_magic_to_max = yes
}

change_character_magic_monthly = {
	if = {
		limit = { NOT = { has_variable = magic } }
		set_variable = {
			name = magic
			value = 0
		}
	}
	change_variable = {
		name = magic
		add = omni_character_monthly_mana_value
	}
	clamp_character_magic_to_max = yes
}

change_country_scope_magic_vars = {
	# --- Country mana ---
	if = {
		limit = {
			NOT = { has_variable = magic }
		}
		set_variable = {
			name = magic
			value = 0
		}
	}
	change_variable = {
		name = magic
		add = modifier:global_cohort_recruit_speed
	}
	if = {
		limit = { var:magic > 100 }
		set_variable = {
			name = magic
			value = 100
		}
	}
	# ------------------------------------------------------------------
	# This effect runs from each country's monthly pulse. every_character is scoped
	# to that country, so its mana tick must not be behind a global cooldown: the
	# first country to claim one otherwise blocks every other country's casters.
	# ------------------------------------------------------------------
	every_character = {
		limit = {
			is_alive = yes
		}
		if = {
			limit = {
				NOT = { has_variable = magic }
			}
			set_variable = {
				name = magic
				value = 0
			}
		}
		# Refresh the display copies (tooltips can only read stored vars in
		# this engine version), apply one month's regen, clamp.
		set_variable = {
			name = magic_monthly
			value = omni_character_monthly_mana_value
		}
		set_variable = {
			name = magic_max
			value = omni_character_magic_max_value
		}
		change_variable = {
			name = magic
			add = omni_character_monthly_mana_value
		}
		if = {
			limit = { var:magic > omni_character_magic_max_value }
			set_variable = {
				name = magic
				value = omni_character_magic_max_value
			}
		}
	}
}

# ---------------------------------------------------------------------------
# REMOVED (July 2026): change_magic_vars.
#
# It was a legacy duplicate of the mana tick that walked every_country,
# every_province AND every_living_character in a single call - three global
# iterators. A whole-tree search confirmed nothing referenced it: not a single
# event, effect, decision or on_action called change_magic_vars, so it had no
# behaviour to preserve. It is deleted rather than commented out because leaving a
# ready-made whole-world sweep lying around invites someone to wire it up.
#
# The live path is: omni_monthly_magic_pulse -> omni_perf.1 ->
# magic_monthly_effects -> change_country_scope_magic_vars (above), which handles
# country and character mana monthly.
# ---------------------------------------------------------------------------
