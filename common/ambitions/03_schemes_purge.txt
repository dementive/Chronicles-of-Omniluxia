scheme_purge_court = {
	can_be_picked = {
		scheme_preconditions_ruler = yes
		is_ruler = yes
		NOT = { has_variable = ongoing_scheme_target }
		wealth >= 100
	}
	finished_when = { custom_tooltip = { text = scheme_ambition_fulfillment_always_yes } }
	duration = 365
	skip_initial_abort = yes
	abort = {
		OR = { is_alive = no is_ruler = no wealth <= 0 has_character_modifier = indisposed_charmod }
	}
	chance = { base = 3 }
	on_monthly = scheme_purge_court_monthly
	on_start = scheme_purge_court_start
	on_finish = scheme_purge_court_finish
	on_abort = scheme_abort
	modifier = { monthly_character_wealth = -2 }
}
