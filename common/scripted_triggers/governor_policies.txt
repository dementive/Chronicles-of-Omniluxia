# state_is_disloyal is used (at state scope) by common/script_values/governor_policies.txt and
# common/scripted_triggers/ai_cities_triggers.txt but was never defined anywhere in this mod, causing
# "Unknown trigger type: state_is_disloyal" errors. Ported from Invictus's common/scripted_triggers/governor_policies.txt.
state_is_disloyal = {
    state_level_loyalty < 33
}
