omni_can_pay_magic_study_cost = {
	OR = {
		AND = {
			scope:target = { is_ruler = yes }
			treasury >= $PRICE$
		}
		AND = {
			scope:target = {
				NOT = { is_ruler = yes }
				wealth >= $PRICE$
			}
		}
	}
}
