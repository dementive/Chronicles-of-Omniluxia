# Helper trigger required by the restored Invictus/Anbeeld AI character module.

tribal_civilization_allow = {
    political_influence >= 100
    treasury >= 50
    centralization >= 50
    capital_scope = { civilization_value >= 40 }
    has_law = oppidi_law_tribal
    NOT = { government = tribal_chiefdom }
}

