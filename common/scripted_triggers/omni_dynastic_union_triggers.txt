omni_dynastic_marriage_available_alert_trigger = {
	is_ai = no
	is_monarchy = yes
	is_subject = no
	exists = current_ruler
	NOT = { has_variable = dynastic_marriage_proposal_ongoing }
	omni_ruler_available_for_court_action_trigger = yes
	current_ruler = {
		is_adult = yes
		is_married = no
		prisoner = no
		is_mercenary = no
	}
	any_country = {
		is_monarchy = yes
		is_subject = no
		has_land = yes
		in_diplomatic_range = yes
		NOR = {
			this = root
			tag = REB
			tag = BAR
		}
		NOT = { has_variable = dynastic_marriage_proposal_ongoing }
		NOT = { is_subject_of = root }
		NOT = {
			any_subject = {
				is_subject_type = dynastic_union_subject
			}
		}
		num_of_cities <= root.num_of_cities
		rank <= root.rank
		opinion = {
			target = root
			value >= 80
		}
		exists = current_ruler
		current_ruler = {
			is_adult = yes
			is_married = no
			prisoner = no
			is_mercenary = no
			NOT = { is_same_gender = root.current_ruler }
		}
	}
}
