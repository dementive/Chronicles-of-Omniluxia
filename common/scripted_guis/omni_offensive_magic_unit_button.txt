omni_offensive_magic_unit_button = {
	scope = unit
	saved_scopes = {
		player
	}

	is_shown = {
		OR = {
			is_army = yes
			is_navy = yes
		}
		unit_owner = {
			NOT = { tag = BAR }
			NOT = { tag = REB }
			NOT = { tag = PIR }
			NOT = { this = scope:player }
			scope:player = {
				war_with = PREV
			}
		}
		exists = commander
		commander = {
			OR = {
				is_general = yes
				is_admiral = yes
			}
			is_alive = yes
			NOT = { has_variable = is_the_revant }
		}
		scope:player = {
			is_ai = no
			any_character = {
				OR = {
					is_ruler = yes
					is_general = yes
					has_office = office_physician
					has_office = office_republic_physician
					has_office = office_apothecary
				}
				omni_character_is_eligible_caster = yes
			}
		}
	}

	ai_is_valid = {
		always = no
	}

	# is_valid drives the ENABLED state. Greyed out when no eligible mage can afford
	# even the cheapest offensive spell (15 MP scaled).
	is_valid = {
		custom_tooltip = {
			text = omni_offensive_magic_unit_button_tooltip
		}
		custom_tooltip = {
			text = omni_unit_magic_need_mana_tt
			scope:player = { omni_player_has_affordable_mage = { BASE = 15 } }
		}
	}

	effect = {
		commander = {
			save_scope_as = target
		}
		scope:player = {
			trigger_event = { id = me_zorgo_magic_focus.8 }
		}
	}
}
