purge_court_abort = {
	scope = character
	saved_scopes = { player }

	is_shown = {
		is_alive = yes
		has_ambition = scheme_purge_court
		employer = scope:player
	}

	is_valid = {
		is_alive = yes
		has_ambition = scheme_purge_court
		employer = scope:player
	}

	effect = {
		clear_ambition_effect = yes
	}
}
