# PERF Round 5: the delayed on_action that lived here (omni_yearly_ai_library_pulse) is gone.
# `delay = { days = { 1 364 } }` in an on_actions list is rolled ONCE per firing, not per
# country, so it did NOT spread the AI scroll shopping - every AI country still shopped on the
# same day. AI library scroll shopping (with its treasury + library gates) now runs inside
# omni_perf.2 (events/omni_perf_pulses.txt), scheduled per-country with a random day offset
# from omni_yearly_country_effect (choo_effects.txt). See PERFORMANCE_AUDIT.md Round 5.
