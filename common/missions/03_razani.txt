03_razani = {
    header = "lotr_tribal_reform_mission_headers"
    icon = "seleukid_2"

    repeatable = no
    chance = 1000

    potential = {
        NOT = { has_variable = mission_cooldown_var }
        tag = EGA
    }

    abort = {}
    on_start = {
        start_mission_ai_effect = yes
    }
    on_abort = {
        custom_tooltip = general_mission_cooldown_tt
        set_variable = {
            name = mission_cooldown_var
            days = 7300
        }
    }
    on_completion = {
        current_ruler = {
            average_martial_effect = yes
        }
    }

    03_razani_task_1 = {
        icon = "task_political"
        duration = 30
        bypass = {
            current_ruler = {
                is_married = yes
            }
        }

        allow = {}
        on_completion = {
            reverse_add_opinion = { target = SOV modifier = opinion_hostile_neighbor }
            trigger_event = me_jqf.1
        }
    }
}
