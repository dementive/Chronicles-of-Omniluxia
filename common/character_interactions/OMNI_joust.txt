omniluxia_joust = {
	on_other_nation = no
	on_own_nation = yes

	message = no

	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"

	potential_trigger = {
		hidden:scope:target = {
			employer = scope:actor
			is_ruler = yes
			is_alive = yes
			is_adult = yes
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
		}
		scope:actor = {
			is_ai = no
			OR = {
				country_culture_group = aralans
				country_culture_group = zani_vetalian
				country_culture_group = rohenoan
			}
			war = no
		}
	}

	allowed_trigger = {
		scope:target = {
			trigger_if = {
				limit = { is_adult = no }
				is_adult = yes
			}
			is_ruler = yes
			wealth >= 100
			custom_tooltip = {
				text = is_on_omni_joust_tt
				NOT = { has_variable = omni_joust_cooldown }
			}
		}
	}

	effect = {
		scope:actor = {
			trigger_event = { id =  me_zorgoomni.43 }
		}
		scope:target = {
			set_variable = {
				name = omni_joust_cooldown
				days = 3650
			}
		}
	}
}
