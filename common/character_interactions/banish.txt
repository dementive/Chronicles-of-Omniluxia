banish_char = {

	on_other_nation = no
	on_own_nation = yes
	close_ui = yes
	needs_senate_approval = yes
	
	sound = "event:/SFX/UI/Character/Unique/sfx_ui_character_banish"
	
	potential_trigger = {
		exists = scope:actor
		exists = scope:target
		hidden:scope:target = {
			employer = scope:actor
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
			is_mercenary = no
			prisoner_valid_for_country_events_trigger = yes
			NOT = {
				has_character_modifier = disloyal_successor_cmod
			}
		}
	}	
	
	allowed_trigger = {
		scope:actor = { omni_ruler_available_for_court_action_trigger = yes }
		exists = scope:target
		hidden:scope:target = {
			is_ruler = no
			prisoner = yes
			is_co_ruler = no
			NOT = { has_culture = ishtari }
		}
	}
	
	country_target_trigger = {
		in_diplomatic_range = scope:actor
		NOT = {
			this = scope:actor
		}
	}
	
	effect = {
		scope:actor = {
			add_tyranny = 1
		}
		scope:target = {
			if = {
				limit = {
					exists = scope:recipient
				}
				banish = scope:recipient
			}
			else = {
				custom_tooltip = "banish_effect_tt"
			}
			if = {	
				limit = { is_minor_character = no }
				family = { add_prestige = -25 }
			}
		}
	}
	
	
	
}	
