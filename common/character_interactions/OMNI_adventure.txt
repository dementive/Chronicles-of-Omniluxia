omniluxia_adventure = {
	on_other_nation = no
	on_own_nation = yes

	message = no

	sound = "event:/SFX/UI/Character/Generic/sfx_ui_character_scholar"

	potential_trigger = {
		always = no # THIS IS DISABLED!!!


		hidden:scope:target = {
			employer = scope:actor
			is_ruler = yes
			is_alive = yes
			is_adult = yes
			NOT = { employer = { tag = REB } }
			NOT = { employer = { tag = BAR } }
		}
		scope:actor = {
			is_ai = no
		}
	}

	allowed_trigger = {
		scope:target = {
			trigger_if = {
				limit = { is_adult = no }
				is_adult = yes
			}
			is_ruler = yes
			wealth >= 500
			custom_tooltip = {
				text = is_on_omni_adventure_tt
				NOT = { has_variable = omni_adventure_cooldown }
			}
		}
	}

	effect = {
		scope:target = {
			set_variable = {
				name = omni_adventure_cooldown
				days = 3650
			}
			employer = {
				save_adventure_companion_scopes = yes
			}
			trigger_event = adventure.1
		}
	}
}
